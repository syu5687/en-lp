# Windows Codex 引き継ぎ指示 — en1150 鹿児島・海洋散骨LP

## 最初に行うこと

Windows版Codexでこのリポジトリを開いたら、最初にこのファイルを読み、以下を実行してください。

```powershell
git pull --ff-only origin main
git status
git log --oneline -5
```

未コミット変更がある場合は内容を確認し、ユーザーや別作業の変更を上書きしないでください。既存の変更を勝手に破棄、復元、コミットへ混在させないでください。

## 案件情報

- リポジトリ: `https://github.com/syu5687/en-lp.git`
- 対象LP: `https://en1150.co.jp/lp1/`
- LP主要ファイル:
  - `lp1/index.php`
  - `lp1/styles.css`
  - `lp1/script.js`
  - `lp1/assets/`
- 公開基盤: PHP 8.1 / Apache / Cloud Run
- リージョン: `asia-northeast1`
- `main`へのpushで`cloudbuild.yaml`が実行され、Cloud Runへ自動デプロイされます。

このファイル作成時点のLP公開バージョンは `v2026091402` です。作業開始時には、本番HTMLの`meta[name="version"]`とGit履歴を再確認してください。

## 現在の完成状態

鹿児島・錦江湾の海洋散骨LPを `/lp1/` で本番公開しています。PCとスマートフォンに対応済みです。

主な掲載内容は次のとおりです。

- 海と桜島を大きく見せるファーストビュー
- 次回合同海洋散骨の日程
- 委託散骨・合同散骨・貸切散骨の3プラン
- 六価クロム検査・無害化
- 手作業による粉骨と粉骨工程ギャラリー
- 錦江湾での散骨
- 緯度・経度と写真入りの散骨証明書
- 手元供養
- 寺院納骨と海洋散骨を組み合わせる `teraumi`
- 寺院での終活・供養セミナー
- 日本海洋散骨協会の加盟事業者表示
- 選ばれる5つの理由、スタッフ、店舗、実施レポート、お客様の声、FAQ
- ページ内お問い合わせ・資料請求フォーム
- 個人情報の管理・保護についての案内

## 正式デザインと表示順

確定デザインは、白・濃紺・海のブルーを基調とした鹿児島版です。和文見出しと実写真を使い、落ち着きと信頼感を優先してください。独自判断で別の配色や一般的な海外風LPへ変更しないでください。

スマートフォンでも世界観と情報順を保ち、横スクロール、文字切れ、固定CTAによる操作妨害がないように調整してください。

## 店舗紹介の確定内容

スタッフ紹介の直後、海洋散骨実施レポートの前に店舗紹介を配置しています。

- ラベル: `店舗でのご相談`
- 見出し: `鹿児島県内に実店舗があります。`
- 本文: `お墓じまい・海洋葬・その後のご供養まで、店舗で直接ご相談いただけます。大切なご遺骨をお預かりする事業者だからこそ、安心してご相談ください。`
- 店舗外観: `lp1/assets/store-exterior.jpg`
- 店内相談スペース: `lp1/assets/store-interior.jpg`

関連コミット:

- `5c5d785` — 店舗紹介枠と画像を追加
- `e68180e` — 店舗紹介の確定文言へ変更

## フォームとCV計測

LP1のCVフォームはページ内に設置済みです。送信内容は既存の管理画面の問い合わせ管理で扱い、LP1経由であることを識別できる設計です。

- フォーム名: `鹿児島・錦江湾 海洋散骨LP（lp1）`
- GA4識別値: `lp1_kagoshima_ocean_burial`
- `資料請求（無料PDF）`を選択した場合、郵便番号と住所を必須にします。
- その他の相談では、郵便番号と住所は任意です。
- 既存の管理画面、問い合わせ保存、通知処理、GA4イベント名を壊さないでください。
- APIキー、HMAC共有鍵、認証情報などを画面・ログ・コミットへ出力しないでください。

## 変更してはいけない前提

- 既存サイトTOPの`h1`と`title`を変更しない。
- 既存URL、canonical、301リダイレクトを変更しない。
- 既存のGA4イベント名を変更しない。
- 山野石材株式会社を競合として扱わない。提携・協力先です。
- 有限会社縁が海洋散骨業務を受託している前提を維持する。
- 価格、実績、日程、スタッフ、電話番号は現在の公式情報で確認できる値だけを使う。
- モック画像内の仮価格・仮日程を本番情報として使用しない。
- 確認できない情報はプレースホルダーまたは未掲載にする。
- Cloud SQLは使用しない。アプリケーションデータはプロジェクト専用のFirebaseを使用し、原則としてCloud Firestoreを選ぶ。
- 既存のFirebase設定を別の新規プロジェクトへ流用しない。

## 作業と公開の手順

1. `git pull --ff-only origin main`で最新化する。
2. `git status`と直近のコミットを確認する。
3. 依頼対象と関係するファイルだけを編集する。
4. HTML、CSS、JavaScript、画像参照、フォーム動作を確認する。
5. PC幅とスマートフォン幅で表示を確認する。
6. リリース時は`lp1/index.php`のReleaseコメント、versionメタ、CSS/JSのキャッシュキーを同じ値へ更新する。
7. 対象ファイルだけをコミットし、`main`へpushする。
8. Cloud Buildの完了を待つ。
9. `https://en1150.co.jp/lp1/?cb=<新しいバージョン>`を開き、本番のversion、文言、画像、フォーム、PC/SP表示を確認する。

公開後は、変更ファイル、実データへ置換した箇所、未確認事項、元デザインから変更した箇所と理由、公開前後の確認結果をユーザーへ報告してください。

## Windows版Codexへ送る開始指示

新しいタスクでは次のように指示してください。

> `WINDOWS_CODEX_HANDOFF.md`を最初に読み、記載された案件情報・制約・公開手順を引き継いでください。`git pull --ff-only origin main`、`git status`、本番LPの状態を確認してから作業を始めてください。

